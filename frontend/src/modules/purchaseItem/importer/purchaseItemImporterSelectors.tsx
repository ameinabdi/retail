import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const purchaseItemImporterSelectors = importerSelectors(
  'purchaseItem.importer',
);

export default purchaseItemImporterSelectors;
