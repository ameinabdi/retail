import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const purchaseImporterSelectors = importerSelectors(
  'purchase.importer',
);

export default purchaseImporterSelectors;
