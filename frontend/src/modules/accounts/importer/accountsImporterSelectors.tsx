import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const accountsImporterSelectors = importerSelectors(
  'accounts.importer',
);

export default accountsImporterSelectors;
